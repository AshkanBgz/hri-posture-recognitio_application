org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Works around a known Windows-specific Gradle/Kotlin bug ("Could not close incremental caches")
# where the incremental-compilation cache files get corrupted/locked mid-build. Costs a bit of
# rebuild speed on incremental builds, but the plain build now completes reliably instead of
# failing after 50+ minutes.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
